# URL of the published Finance Dashboard web app (no trailing slash on
# the host). The WebView in app/index.tsx targets APP_URL — override the
# constant directly there if you want to point at a staging environment.
EXPO_PUBLIC_APP_URL=https://stevenmkay.github.io/Finance-Dashboard/dashboard.html
